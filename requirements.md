1. create me an electron.js app. 
2. the app should communicate to more than 1 hardware device over modbus tcp/ip.
3. the app read/writes coils and registers. It should display all of those values. 
4. There should be different type of controls which resembles mechanical controls in a physical instrument.
5. It should also have utility to save all the recorded data and should record data.
6. It should have presets which when loaded will do a sequential writes to the different devices to simulate different scenarios. 
7. It should also have an ability to calibrate different parameters in any of the hardware device. 
8. There should be proper application logging (using rotating ) so that when application crash, using the logs it can be determined where it crashed.  
9. app should have proper error handling. 
